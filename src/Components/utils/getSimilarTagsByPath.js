//Components
import getAllListingTagsByListingId from './getAllListingTagsByListingId'
import findTagIdByName from './findTagIdByName'

//Supports single-level similar tags based on listings.
export default function getSimilarTagsByPath(props,path) {
  return 'x';
}